This repo is to use fashion mnist dataset to explore deep learning model.

including technics:
* simple deep neural network
* bias and variance explored by cross validation
* dropout regularization
* early stopping
* kfold average
* conv neural network
* batch normalization
